version: '3.8'

services:
  server:
    build: 
      context: .
      dockerfile: apps/server/Dockerfile
    ports:
      - "3001:3001"
    volumes:
      - server-data:/usr/src/app/apps/server/data
    environment:
      - NODE_ENV=production
      - PORT=3001

volumes:
  server-data:
